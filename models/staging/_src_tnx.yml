version: 2

sources:
  - name: crypto_ethereum
    database: bigquery-public-data
    tables:
      - name: transactions
        config: 
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 2
              period: day
          loaded_at_field: block_timestamp
